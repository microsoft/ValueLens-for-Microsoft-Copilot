name: sync-shared

on:
  push:
    paths:
      - '3. Fabric/pipelines/**'
      - '3. Fabric/notebooks/**'
      - '3. Fabric/archive/extended/**/notebooks/_core/**'
      - '3. Fabric/archive/flows/**'
      - '3. Fabric/*.pbit'
      - 'tests/**'
      - 'scripts/sync-shared.ps1'
      - '.github/workflows/sync-shared.yml'
  pull_request:
    paths:
      - '3. Fabric/pipelines/**'
      - '3. Fabric/notebooks/**'
      - '3. Fabric/archive/extended/**/notebooks/_core/**'
      - '3. Fabric/archive/flows/**'
      - '3. Fabric/*.pbit'
      - 'tests/**'
      - 'scripts/sync-shared.ps1'
      - '.github/workflows/sync-shared.yml'

permissions:
  contents: read

jobs:
  check-shared:
    name: Verify archived _core notebooks match 3. Fabric/notebooks
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Run sync-shared in check mode
        shell: pwsh
        run: |
          ./scripts/sync-shared.ps1 -Check
